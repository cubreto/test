openai>=1.0        # OpenAI-compatible client (Together AI via base_url, or OpenAI)
numpy>=1.24        # embedding similarity
scikit-learn>=1.3  # offline TF-IDF retriever (no network/key needed)
anthropic>=0.40    # only needed for --provider anthropic
